---
title: "Описательная статистика"
author: "SVA"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document:
    code_folding: hide
    df_print: paged
    toc: yes
    toc_depth: 4
    toc_float:
      collapsed: no
  word_document:
    toc: yes
    toc_depth: '4'
editor_options:
  chunk_output_type: inline
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  include = TRUE,
  message = FALSE, 
  warning = FALSE
)

rm(list=ls())
source("functions.R")
```


```{r}
df <- read.csv("./data/accuracy_features.csv")

df$acc_max[is.infinite(df$acc_max)] <- NA
df$acc_range[is.infinite(df$acc_range)] <- NA
df$X <- NULL

```


# Описательные статистики


















### ratio {.tabset .tabset-fade .tabset-pills}
**ratio**

#### Tables

Распределение по переменной "ratio"
```{r}
table(df$ratio)
```

Распределение по переменной "ratio", %
```{r}
round(prop.table(table(df$ratio)) * 100, 2)
```





### time_total {.tabset .tabset-fade .tabset-pills}
**Общее время выполнения**

#### Descriptive stat
```{r}
quantile(df$time_total, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_total))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_total"
plot_list <- draw_plots_continuous(df, var = df$time_total, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_total, 1.5)
outliers[order(outliers$time_total, decreasing = T),]
outliers[order(outliers$time_total),]
```






### time_mean {.tabset .tabset-fade .tabset-pills}
**Среднее время на клик**

#### Descriptive stat
```{r}
quantile(df$time_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_mean"
plot_list <- draw_plots_continuous(df, var = df$time_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_mean, 1.5)
outliers[order(outliers$time_mean, decreasing = T),]
outliers[order(outliers$time_mean),]
```






### time_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение времени на клик**

#### Descriptive stat
```{r}
quantile(df$time_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_sd"
plot_list <- draw_plots_continuous(df, var = df$time_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_sd, 1.5)
outliers[order(outliers$time_sd, decreasing = T),]
outliers[order(outliers$time_sd),]
```






### time_median {.tabset .tabset-fade .tabset-pills}
**Медианное время на клик**

#### Descriptive stat
```{r}
quantile(df$time_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_median"
plot_list <- draw_plots_continuous(df, var = df$time_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_median, 1.5)
outliers[order(outliers$time_median, decreasing = T),]
outliers[order(outliers$time_median),]
```






### time_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее времени на клик**

#### Descriptive stat
```{r}
quantile(df$time_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_trimmed"
plot_list <- draw_plots_continuous(df, var = df$time_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_trimmed, 1.5)
outliers[order(outliers$time_trimmed, decreasing = T),]
outliers[order(outliers$time_trimmed),]
```






### time_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение времени на клик**

#### Descriptive stat
```{r}
quantile(df$time_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_mad"
plot_list <- draw_plots_continuous(df, var = df$time_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_mad, 1.5)
outliers[order(outliers$time_mad, decreasing = T),]
outliers[order(outliers$time_mad),]
```






### time_min {.tabset .tabset-fade .tabset-pills}
**Минимальное время клика**

#### Descriptive stat
```{r}
quantile(df$time_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_min"
plot_list <- draw_plots_continuous(df, var = df$time_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_min, 1.5)
outliers[order(outliers$time_min, decreasing = T),]
outliers[order(outliers$time_min),]
```






### time_max {.tabset .tabset-fade .tabset-pills}
**Максимальное время клика**

#### Descriptive stat
```{r}
quantile(df$time_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_max"
plot_list <- draw_plots_continuous(df, var = df$time_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_max, 1.5)
outliers[order(outliers$time_max, decreasing = T),]
outliers[order(outliers$time_max),]
```






### time_range {.tabset .tabset-fade .tabset-pills}
**Размах времени на клик**

#### Descriptive stat
```{r}
quantile(df$time_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_range"
plot_list <- draw_plots_continuous(df, var = df$time_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_range, 1.5)
outliers[order(outliers$time_range, decreasing = T),]
outliers[order(outliers$time_range),]
```






### time_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_skew"
plot_list <- draw_plots_continuous(df, var = df$time_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_skew, 1.5)
outliers[order(outliers$time_skew, decreasing = T),]
outliers[order(outliers$time_skew),]
```






### time_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$time_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_kurtosis, 1.5)
outliers[order(outliers$time_kurtosis, decreasing = T),]
outliers[order(outliers$time_kurtosis),]
```






### time_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка среднего времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_se"
plot_list <- draw_plots_continuous(df, var = df$time_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_se, 1.5)
outliers[order(outliers$time_se, decreasing = T),]
outliers[order(outliers$time_se),]
```






### time_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$time_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_Q0.25, 1.5)
outliers[order(outliers$time_Q0.25, decreasing = T),]
outliers[order(outliers$time_Q0.25),]
```






### time_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$time_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_Q0.75, 1.5)
outliers[order(outliers$time_Q0.75, decreasing = T),]
outliers[order(outliers$time_Q0.75),]
```






### time_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд времени выполнения**

#### Descriptive stat
```{r}
quantile(df$time_trend, na.rm = T)
```

```{r}
data.frame(describeBy(df$time_trend))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_trend"
plot_list <- draw_plots_continuous(df, var = df$time_trend, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$time_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df$time_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = time_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$time_trend, 1.5)
outliers[order(outliers$time_trend, decreasing = T),]
outliers[order(outliers$time_trend),]
```






### fitts_a {.tabset .tabset-fade .tabset-pills}
**Коэффициент a в формуле Фиттса**

#### Descriptive stat
```{r}
quantile(df$fitts_a, na.rm = T)
```

```{r}
data.frame(describeBy(df$fitts_a))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "fitts_a"
plot_list <- draw_plots_continuous(df, var = df$fitts_a, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$fitts_a);print(st)
check_normal_distribution(st)

ks <- ks.test(df$fitts_a, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = fitts_a)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - fitts_a", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$fitts_a, 1.5)
outliers[order(outliers$fitts_a, decreasing = T),]
outliers[order(outliers$fitts_a),]
```






### fitts_b {.tabset .tabset-fade .tabset-pills}
**Коэффициент b в формуле Фиттса**

#### Descriptive stat
```{r}
quantile(df$fitts_b, na.rm = T)
```

```{r}
data.frame(describeBy(df$fitts_b))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "fitts_b"
plot_list <- draw_plots_continuous(df, var = df$fitts_b, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$fitts_b);print(st)
check_normal_distribution(st)

ks <- ks.test(df$fitts_b, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = fitts_b)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - fitts_b", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$fitts_b, 1.5)
outliers[order(outliers$fitts_b, decreasing = T),]
outliers[order(outliers$fitts_b),]
```






### distance_total {.tabset .tabset-fade .tabset-pills}
**Общая амплитуда**

#### Descriptive stat
```{r}
quantile(df$distance_total, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_total))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_total"
plot_list <- draw_plots_continuous(df, var = df$distance_total, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_total, 1.5)
outliers[order(outliers$distance_total, decreasing = T),]
outliers[order(outliers$distance_total),]
```






### distance_mean {.tabset .tabset-fade .tabset-pills}
**Средняя амплитуда на клик**

#### Descriptive stat
```{r}
quantile(df$distance_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_mean"
plot_list <- draw_plots_continuous(df, var = df$distance_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_mean, 1.5)
outliers[order(outliers$distance_mean, decreasing = T),]
outliers[order(outliers$distance_mean),]
```






### distance_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение амплитуда на клик**

#### Descriptive stat
```{r}
quantile(df$distance_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_sd"
plot_list <- draw_plots_continuous(df, var = df$distance_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_sd, 1.5)
outliers[order(outliers$distance_sd, decreasing = T),]
outliers[order(outliers$distance_sd),]
```






### distance_median {.tabset .tabset-fade .tabset-pills}
**Медианная амплитуда на клик**

#### Descriptive stat
```{r}
quantile(df$distance_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_median"
plot_list <- draw_plots_continuous(df, var = df$distance_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_median, 1.5)
outliers[order(outliers$distance_median, decreasing = T),]
outliers[order(outliers$distance_median),]
```






### distance_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее амплитуды на клик**

#### Descriptive stat
```{r}
quantile(df$distance_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_trimmed"
plot_list <- draw_plots_continuous(df, var = df$distance_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_trimmed, 1.5)
outliers[order(outliers$distance_trimmed, decreasing = T),]
outliers[order(outliers$distance_trimmed),]
```






### distance_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение амплитуды на клик**

#### Descriptive stat
```{r}
quantile(df$distance_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_mad"
plot_list <- draw_plots_continuous(df, var = df$distance_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_mad, 1.5)
outliers[order(outliers$distance_mad, decreasing = T),]
outliers[order(outliers$distance_mad),]
```






### distance_min {.tabset .tabset-fade .tabset-pills}
**Минимальная амплитуда клика**

#### Descriptive stat
```{r}
quantile(df$distance_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_min"
plot_list <- draw_plots_continuous(df, var = df$distance_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_min, 1.5)
outliers[order(outliers$distance_min, decreasing = T),]
outliers[order(outliers$distance_min),]
```






### distance_max {.tabset .tabset-fade .tabset-pills}
**Максимальная амплитуда клика**

#### Descriptive stat
```{r}
quantile(df$distance_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_max"
plot_list <- draw_plots_continuous(df, var = df$distance_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_max, 1.5)
outliers[order(outliers$distance_max, decreasing = T),]
outliers[order(outliers$distance_max),]
```






### distance_range {.tabset .tabset-fade .tabset-pills}
**Размах амплитуды на клик**

#### Descriptive stat
```{r}
quantile(df$distance_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_range"
plot_list <- draw_plots_continuous(df, var = df$distance_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_range, 1.5)
outliers[order(outliers$distance_range, decreasing = T),]
outliers[order(outliers$distance_range),]
```






### distance_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_skew"
plot_list <- draw_plots_continuous(df, var = df$distance_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_skew, 1.5)
outliers[order(outliers$distance_skew, decreasing = T),]
outliers[order(outliers$distance_skew),]
```






### distance_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$distance_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_kurtosis, 1.5)
outliers[order(outliers$distance_kurtosis, decreasing = T),]
outliers[order(outliers$distance_kurtosis),]
```






### distance_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка среднего амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_se"
plot_list <- draw_plots_continuous(df, var = df$distance_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_se, 1.5)
outliers[order(outliers$distance_se, decreasing = T),]
outliers[order(outliers$distance_se),]
```






### distance_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$distance_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_Q0.25, 1.5)
outliers[order(outliers$distance_Q0.25, decreasing = T),]
outliers[order(outliers$distance_Q0.25),]
```






### distance_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$distance_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_Q0.75, 1.5)
outliers[order(outliers$distance_Q0.75, decreasing = T),]
outliers[order(outliers$distance_Q0.75),]
```






### distance_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд амплитуды**

#### Descriptive stat
```{r}
quantile(df$distance_trend, na.rm = T)
```

```{r}
data.frame(describeBy(df$distance_trend))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_trend"
plot_list <- draw_plots_continuous(df, var = df$distance_trend, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$distance_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df$distance_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = distance_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$distance_trend, 1.5)
outliers[order(outliers$distance_trend, decreasing = T),]
outliers[order(outliers$distance_trend),]
```






### pause_total {.tabset .tabset-fade .tabset-pills}
**Общее время пауз**

#### Descriptive stat
```{r}
quantile(df$pause_total, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_total))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_total"
plot_list <- draw_plots_continuous(df, var = df$pause_total, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_total, 1.5)
outliers[order(outliers$pause_total, decreasing = T),]
outliers[order(outliers$pause_total),]
```






### pause_number {.tabset .tabset-fade .tabset-pills}
**Количество пауз**

#### Descriptive stat
```{r}
quantile(df$pause_number, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_number))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_number"
plot_list <- draw_plots_continuous(df, var = df$pause_number, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_number);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_number, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_number)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_number", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_number, 1.5)
outliers[order(outliers$pause_number, decreasing = T),]
outliers[order(outliers$pause_number),]
```






### pause_mean {.tabset .tabset-fade .tabset-pills}
**Среднее пауза**

#### Descriptive stat
```{r}
quantile(df$pause_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_mean"
plot_list <- draw_plots_continuous(df, var = df$pause_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_mean, 1.5)
outliers[order(outliers$pause_mean, decreasing = T),]
outliers[order(outliers$pause_mean),]
```






### pause_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение пауз**

#### Descriptive stat
```{r}
quantile(df$pause_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_sd"
plot_list <- draw_plots_continuous(df, var = df$pause_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_sd, 1.5)
outliers[order(outliers$pause_sd, decreasing = T),]
outliers[order(outliers$pause_sd),]
```






### pause_median {.tabset .tabset-fade .tabset-pills}
**Медианная пауза**

#### Descriptive stat
```{r}
quantile(df$pause_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_median"
plot_list <- draw_plots_continuous(df, var = df$pause_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_median, 1.5)
outliers[order(outliers$pause_median, decreasing = T),]
outliers[order(outliers$pause_median),]
```






### pause_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее пауз**

#### Descriptive stat
```{r}
quantile(df$pause_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_trimmed"
plot_list <- draw_plots_continuous(df, var = df$pause_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_trimmed, 1.5)
outliers[order(outliers$pause_trimmed, decreasing = T),]
outliers[order(outliers$pause_trimmed),]
```






### pause_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение пауз**

#### Descriptive stat
```{r}
quantile(df$pause_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_mad"
plot_list <- draw_plots_continuous(df, var = df$pause_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_mad, 1.5)
outliers[order(outliers$pause_mad, decreasing = T),]
outliers[order(outliers$pause_mad),]
```






### pause_min {.tabset .tabset-fade .tabset-pills}
**Минимальное время паузы**

#### Descriptive stat
```{r}
quantile(df$pause_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_min"
plot_list <- draw_plots_continuous(df, var = df$pause_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_min, 1.5)
outliers[order(outliers$pause_min, decreasing = T),]
outliers[order(outliers$pause_min),]
```






### pause_max {.tabset .tabset-fade .tabset-pills}
**Максимальное время паузы**

#### Descriptive stat
```{r}
quantile(df$pause_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_max"
plot_list <- draw_plots_continuous(df, var = df$pause_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_max, 1.5)
outliers[order(outliers$pause_max, decreasing = T),]
outliers[order(outliers$pause_max),]
```






### pause_range {.tabset .tabset-fade .tabset-pills}
**Размах пауз**

#### Descriptive stat
```{r}
quantile(df$pause_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_range"
plot_list <- draw_plots_continuous(df, var = df$pause_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_range, 1.5)
outliers[order(outliers$pause_range, decreasing = T),]
outliers[order(outliers$pause_range),]
```






### pause_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии пауз**

#### Descriptive stat
```{r}
quantile(df$pause_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_skew"
plot_list <- draw_plots_continuous(df, var = df$pause_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_skew, 1.5)
outliers[order(outliers$pause_skew, decreasing = T),]
outliers[order(outliers$pause_skew),]
```






### pause_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс пауз**

#### Descriptive stat
```{r}
quantile(df$pause_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$pause_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_kurtosis, 1.5)
outliers[order(outliers$pause_kurtosis, decreasing = T),]
outliers[order(outliers$pause_kurtosis),]
```






### pause_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка пауз**

#### Descriptive stat
```{r}
quantile(df$pause_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_se"
plot_list <- draw_plots_continuous(df, var = df$pause_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_se, 1.5)
outliers[order(outliers$pause_se, decreasing = T),]
outliers[order(outliers$pause_se),]
```






### pause_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль пауз**

#### Descriptive stat
```{r}
quantile(df$pause_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$pause_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_Q0.25, 1.5)
outliers[order(outliers$pause_Q0.25, decreasing = T),]
outliers[order(outliers$pause_Q0.25),]
```






### pause_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль пауз**

#### Descriptive stat
```{r}
quantile(df$pause_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$pause_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_Q0.75, 1.5)
outliers[order(outliers$pause_Q0.75, decreasing = T),]
outliers[order(outliers$pause_Q0.75),]
```






### pause_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд пауз**

#### Descriptive stat
```{r}
quantile(df$pause_trend, na.rm = T)
```

```{r}
data.frame(describeBy(df$pause_trend))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_trend"
plot_list <- draw_plots_continuous(df, var = df$pause_trend, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$pause_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df$pause_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = pause_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$pause_trend, 1.5)
outliers[order(outliers$pause_trend, decreasing = T),]
outliers[order(outliers$pause_trend),]
```






### stops {.tabset .tabset-fade .tabset-pills}
**Количество остановок**

#### Descriptive stat
```{r}
quantile(df$stops, na.rm = T)
```

```{r}
data.frame(describeBy(df$stops))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "stops"
plot_list <- draw_plots_continuous(df, var = df$stops, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$stops);print(st)
check_normal_distribution(st)

ks <- ks.test(df$stops, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = stops)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - stops", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$stops, 1.5)
outliers[order(outliers$stops, decreasing = T),]
outliers[order(outliers$stops),]
```






### velocity_mean {.tabset .tabset-fade .tabset-pills}
**Средняя гармоническая скорость**

#### Descriptive stat
```{r}
quantile(df$velocity_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_mean"
plot_list <- draw_plots_continuous(df, var = df$velocity_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_mean, 1.5)
outliers[order(outliers$velocity_mean, decreasing = T),]
outliers[order(outliers$velocity_mean),]
```






### velocity_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_sd"
plot_list <- draw_plots_continuous(df, var = df$velocity_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_sd, 1.5)
outliers[order(outliers$velocity_sd, decreasing = T),]
outliers[order(outliers$velocity_sd),]
```






### velocity_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_mad"
plot_list <- draw_plots_continuous(df, var = df$velocity_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_mad, 1.5)
outliers[order(outliers$velocity_mad, decreasing = T),]
outliers[order(outliers$velocity_mad),]
```






### velocity_min {.tabset .tabset-fade .tabset-pills}
**Минимальная скорость**

#### Descriptive stat
```{r}
quantile(df$velocity_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_min"
plot_list <- draw_plots_continuous(df, var = df$velocity_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_min, 1.5)
outliers[order(outliers$velocity_min, decreasing = T),]
outliers[order(outliers$velocity_min),]
```






### velocity_max {.tabset .tabset-fade .tabset-pills}
**Максимальная скорость**

#### Descriptive stat
```{r}
quantile(df$velocity_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_max"
plot_list <- draw_plots_continuous(df, var = df$velocity_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_max, 1.5)
outliers[order(outliers$velocity_max, decreasing = T),]
outliers[order(outliers$velocity_max),]
```






### velocity_range {.tabset .tabset-fade .tabset-pills}
**Размах скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_range"
plot_list <- draw_plots_continuous(df, var = df$velocity_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_range, 1.5)
outliers[order(outliers$velocity_range, decreasing = T),]
outliers[order(outliers$velocity_range),]
```






### velocity_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_skew"
plot_list <- draw_plots_continuous(df, var = df$velocity_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_skew, 1.5)
outliers[order(outliers$velocity_skew, decreasing = T),]
outliers[order(outliers$velocity_skew),]
```






### velocity_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$velocity_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_kurtosis, 1.5)
outliers[order(outliers$velocity_kurtosis, decreasing = T),]
outliers[order(outliers$velocity_kurtosis),]
```






### velocity_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_se"
plot_list <- draw_plots_continuous(df, var = df$velocity_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_se, 1.5)
outliers[order(outliers$velocity_se, decreasing = T),]
outliers[order(outliers$velocity_se),]
```






### velocity_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$velocity_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_Q0.25, 1.5)
outliers[order(outliers$velocity_Q0.25, decreasing = T),]
outliers[order(outliers$velocity_Q0.25),]
```






### velocity_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$velocity_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_Q0.75, 1.5)
outliers[order(outliers$velocity_Q0.75, decreasing = T),]
outliers[order(outliers$velocity_Q0.75),]
```






### velocity_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд скорости**

#### Descriptive stat
```{r}
quantile(df$velocity_trend, na.rm = T)
```

```{r}
data.frame(describeBy(df$velocity_trend))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_trend"
plot_list <- draw_plots_continuous(df, var = df$velocity_trend, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$velocity_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df$velocity_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = velocity_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$velocity_trend, 1.5)
outliers[order(outliers$velocity_trend, decreasing = T),]
outliers[order(outliers$velocity_trend),]
```






### acc_n {.tabset .tabset-fade .tabset-pills}
**Количество ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_n, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_n))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_n"
plot_list <- draw_plots_continuous(df, var = df$acc_n, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_n);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_n, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_n)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_n", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_n, 1.5)
outliers[order(outliers$acc_n, decreasing = T),]
outliers[order(outliers$acc_n),]
```











### acc_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_sd"
plot_list <- draw_plots_continuous(df, var = df$acc_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_sd, 1.5)
outliers[order(outliers$acc_sd, decreasing = T),]
outliers[order(outliers$acc_sd),]
```






### acc_median {.tabset .tabset-fade .tabset-pills}
**Медианная ускорение**

#### Descriptive stat
```{r}
quantile(df$acc_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_median"
plot_list <- draw_plots_continuous(df, var = df$acc_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_median, 1.5)
outliers[order(outliers$acc_median, decreasing = T),]
outliers[order(outliers$acc_median),]
```






### acc_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее ускорения**

#### Descriptive stat
```{r}
quantile(df$acc_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_trimmed"
plot_list <- draw_plots_continuous(df, var = df$acc_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_trimmed, 1.5)
outliers[order(outliers$acc_trimmed, decreasing = T),]
outliers[order(outliers$acc_trimmed),]
```






### acc_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_mad"
plot_list <- draw_plots_continuous(df, var = df$acc_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_mad, 1.5)
outliers[order(outliers$acc_mad, decreasing = T),]
outliers[order(outliers$acc_mad),]
```






### acc_min {.tabset .tabset-fade .tabset-pills}
**Минимальное ускорение**

#### Descriptive stat
```{r}
quantile(df$acc_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_min"
plot_list <- draw_plots_continuous(df, var = df$acc_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_min, 1.5)
outliers[order(outliers$acc_min, decreasing = T),]
outliers[order(outliers$acc_min),]
```






### acc_max {.tabset .tabset-fade .tabset-pills}
**Максимальное ускорение**

#### Descriptive stat
```{r}
quantile(df$acc_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_max"
plot_list <- draw_plots_continuous(df, var = df$acc_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_max, 1.5)
outliers[order(outliers$acc_max, decreasing = T),]
outliers[order(outliers$acc_max),]
```






### acc_range {.tabset .tabset-fade .tabset-pills}
**Размах ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_range"
plot_list <- draw_plots_continuous(df, var = df$acc_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_range, 1.5)
outliers[order(outliers$acc_range, decreasing = T),]
outliers[order(outliers$acc_range),]
```






### acc_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_skew"
plot_list <- draw_plots_continuous(df, var = df$acc_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_skew, 1.5)
outliers[order(outliers$acc_skew, decreasing = T),]
outliers[order(outliers$acc_skew),]
```






### acc_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$acc_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_kurtosis, 1.5)
outliers[order(outliers$acc_kurtosis, decreasing = T),]
outliers[order(outliers$acc_kurtosis),]
```






### acc_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_se"
plot_list <- draw_plots_continuous(df, var = df$acc_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_se, 1.5)
outliers[order(outliers$acc_se, decreasing = T),]
outliers[order(outliers$acc_se),]
```






### acc_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$acc_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_Q0.25, 1.5)
outliers[order(outliers$acc_Q0.25, decreasing = T),]
outliers[order(outliers$acc_Q0.25),]
```






### acc_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$acc_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_Q0.75, 1.5)
outliers[order(outliers$acc_Q0.75, decreasing = T),]
outliers[order(outliers$acc_Q0.75),]
```






### jerk_line {.tabset .tabset-fade .tabset-pills}
**Линия рывков**

#### Descriptive stat
```{r}
quantile(df$jerk_line, na.rm = T)
```

```{r}
data.frame(describeBy(df$jerk_line))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "jerk_line"
plot_list <- draw_plots_continuous(df, var = df$jerk_line, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$jerk_line);print(st)
check_normal_distribution(st)

ks <- ks.test(df$jerk_line, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = jerk_line)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - jerk_line", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$jerk_line, 1.5)
outliers[order(outliers$jerk_line, decreasing = T),]
outliers[order(outliers$jerk_line),]
```






### n_jerk {.tabset .tabset-fade .tabset-pills}
**Количество рывков**

#### Descriptive stat
```{r}
quantile(df$n_jerk, na.rm = T)
```

```{r}
data.frame(describeBy(df$n_jerk))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "n_jerk"
plot_list <- draw_plots_continuous(df, var = df$n_jerk, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$n_jerk);print(st)
check_normal_distribution(st)

ks <- ks.test(df$n_jerk, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = n_jerk)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - n_jerk", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$n_jerk, 1.5)
outliers[order(outliers$n_jerk, decreasing = T),]
outliers[order(outliers$n_jerk),]
```






### dec_n {.tabset .tabset-fade .tabset-pills}
**Количество замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_n, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_n))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_n"
plot_list <- draw_plots_continuous(df, var = df$dec_n, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_n);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_n, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_n)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_n", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_n, 1.5)
outliers[order(outliers$dec_n, decreasing = T),]
outliers[order(outliers$dec_n),]
```






### dec_mean {.tabset .tabset-fade .tabset-pills}
**Среднее замедление**

#### Descriptive stat
```{r}
quantile(df$dec_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_mean"
plot_list <- draw_plots_continuous(df, var = df$dec_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_mean, 1.5)
outliers[order(outliers$dec_mean, decreasing = T),]
outliers[order(outliers$dec_mean),]
```






### dec_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_sd"
plot_list <- draw_plots_continuous(df, var = df$dec_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_sd, 1.5)
outliers[order(outliers$dec_sd, decreasing = T),]
outliers[order(outliers$dec_sd),]
```






### dec_median {.tabset .tabset-fade .tabset-pills}
**Медианная замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_median"
plot_list <- draw_plots_continuous(df, var = df$dec_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_median, 1.5)
outliers[order(outliers$dec_median, decreasing = T),]
outliers[order(outliers$dec_median),]
```






### dec_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_trimmed"
plot_list <- draw_plots_continuous(df, var = df$dec_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_trimmed, 1.5)
outliers[order(outliers$dec_trimmed, decreasing = T),]
outliers[order(outliers$dec_trimmed),]
```






### dec_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_mad"
plot_list <- draw_plots_continuous(df, var = df$dec_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_mad, 1.5)
outliers[order(outliers$dec_mad, decreasing = T),]
outliers[order(outliers$dec_mad),]
```






### dec_min {.tabset .tabset-fade .tabset-pills}
**Минимальное замедление**

#### Descriptive stat
```{r}
quantile(df$dec_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_min"
plot_list <- draw_plots_continuous(df, var = df$dec_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_min, 1.5)
outliers[order(outliers$dec_min, decreasing = T),]
outliers[order(outliers$dec_min),]
```











### dec_range {.tabset .tabset-fade .tabset-pills}
**Размах замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_range"
plot_list <- draw_plots_continuous(df, var = df$dec_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_range, 1.5)
outliers[order(outliers$dec_range, decreasing = T),]
outliers[order(outliers$dec_range),]
```






### dec_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_skew"
plot_list <- draw_plots_continuous(df, var = df$dec_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_skew, 1.5)
outliers[order(outliers$dec_skew, decreasing = T),]
outliers[order(outliers$dec_skew),]
```






### dec_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$dec_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_kurtosis, 1.5)
outliers[order(outliers$dec_kurtosis, decreasing = T),]
outliers[order(outliers$dec_kurtosis),]
```






### dec_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_se"
plot_list <- draw_plots_continuous(df, var = df$dec_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_se, 1.5)
outliers[order(outliers$dec_se, decreasing = T),]
outliers[order(outliers$dec_se),]
```






### dec_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$dec_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_Q0.25, 1.5)
outliers[order(outliers$dec_Q0.25, decreasing = T),]
outliers[order(outliers$dec_Q0.25),]
```






### dec_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль замедлений**

#### Descriptive stat
```{r}
quantile(df$dec_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$dec_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$dec_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$dec_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$dec_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = dec_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$dec_Q0.75, 1.5)
outliers[order(outliers$dec_Q0.75, decreasing = T),]
outliers[order(outliers$dec_Q0.75),]
```






### braking_line {.tabset .tabset-fade .tabset-pills}
**Линия резких торможений**

#### Descriptive stat
```{r}
quantile(df$braking_line, na.rm = T)
```

```{r}
data.frame(describeBy(df$braking_line))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "braking_line"
plot_list <- draw_plots_continuous(df, var = df$braking_line, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$braking_line);print(st)
check_normal_distribution(st)

ks <- ks.test(df$braking_line, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = braking_line)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - braking_line", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$braking_line, 1.5)
outliers[order(outliers$braking_line, decreasing = T),]
outliers[order(outliers$braking_line),]
```






### n_braking {.tabset .tabset-fade .tabset-pills}
**Количество резких торможений**

#### Descriptive stat
```{r}
quantile(df$n_braking, na.rm = T)
```

```{r}
data.frame(describeBy(df$n_braking))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "n_braking"
plot_list <- draw_plots_continuous(df, var = df$n_braking, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$n_braking);print(st)
check_normal_distribution(st)

ks <- ks.test(df$n_braking, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = n_braking)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - n_braking", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$n_braking, 1.5)
outliers[order(outliers$n_braking, decreasing = T),]
outliers[order(outliers$n_braking),]
```






### acc_per_sec_mean {.tabset .tabset-fade .tabset-pills}
**Среднее количество ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_mean"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_mean, 1.5)
outliers[order(outliers$acc_per_sec_mean, decreasing = T),]
outliers[order(outliers$acc_per_sec_mean),]
```






### acc_per_sec_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение количества ускорений за секунду **

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_sd"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_sd, 1.5)
outliers[order(outliers$acc_per_sec_sd, decreasing = T),]
outliers[order(outliers$acc_per_sec_sd),]
```






### acc_per_sec_median {.tabset .tabset-fade .tabset-pills}
**Медианное количество ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_median"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_median, 1.5)
outliers[order(outliers$acc_per_sec_median, decreasing = T),]
outliers[order(outliers$acc_per_sec_median),]
```






### acc_per_sec_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_trimmed"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_trimmed, 1.5)
outliers[order(outliers$acc_per_sec_trimmed, decreasing = T),]
outliers[order(outliers$acc_per_sec_trimmed),]
```






### acc_per_sec_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_mad"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_mad, 1.5)
outliers[order(outliers$acc_per_sec_mad, decreasing = T),]
outliers[order(outliers$acc_per_sec_mad),]
```






### acc_per_sec_min {.tabset .tabset-fade .tabset-pills}
**Минимальное количество ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_min"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_min, 1.5)
outliers[order(outliers$acc_per_sec_min, decreasing = T),]
outliers[order(outliers$acc_per_sec_min),]
```






### acc_per_sec_max {.tabset .tabset-fade .tabset-pills}
**Максимальное количество ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_max"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_max, 1.5)
outliers[order(outliers$acc_per_sec_max, decreasing = T),]
outliers[order(outliers$acc_per_sec_max),]
```






### acc_per_sec_range {.tabset .tabset-fade .tabset-pills}
**Размах количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_range"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_range, 1.5)
outliers[order(outliers$acc_per_sec_range, decreasing = T),]
outliers[order(outliers$acc_per_sec_range),]
```






### acc_per_sec_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_skew"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_skew, 1.5)
outliers[order(outliers$acc_per_sec_skew, decreasing = T),]
outliers[order(outliers$acc_per_sec_skew),]
```






### acc_per_sec_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_kurtosis, 1.5)
outliers[order(outliers$acc_per_sec_kurtosis, decreasing = T),]
outliers[order(outliers$acc_per_sec_kurtosis),]
```






### acc_per_sec_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_se"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_se, 1.5)
outliers[order(outliers$acc_per_sec_se, decreasing = T),]
outliers[order(outliers$acc_per_sec_se),]
```






### acc_per_sec_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_Q0.25, 1.5)
outliers[order(outliers$acc_per_sec_Q0.25, decreasing = T),]
outliers[order(outliers$acc_per_sec_Q0.25),]
```






### acc_per_sec_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль количества ускорений за секунду**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_Q0.75, 1.5)
outliers[order(outliers$acc_per_sec_Q0.75, decreasing = T),]
outliers[order(outliers$acc_per_sec_Q0.75),]
```






### acc_per_sec_max_pos {.tabset .tabset-fade .tabset-pills}
**Позиция максимального количества ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_max_pos, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_max_pos))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_max_pos"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_max_pos, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_max_pos);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_max_pos, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_max_pos)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_max_pos", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_max_pos, 1.5)
outliers[order(outliers$acc_per_sec_max_pos, decreasing = T),]
outliers[order(outliers$acc_per_sec_max_pos),]
```






### acc_per_sec_min_pos {.tabset .tabset-fade .tabset-pills}
**Позиция минимального количества ускорений**

#### Descriptive stat
```{r}
quantile(df$acc_per_sec_min_pos, na.rm = T)
```

```{r}
data.frame(describeBy(df$acc_per_sec_min_pos))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_min_pos"
plot_list <- draw_plots_continuous(df, var = df$acc_per_sec_min_pos, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$acc_per_sec_min_pos);print(st)
check_normal_distribution(st)

ks <- ks.test(df$acc_per_sec_min_pos, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = acc_per_sec_min_pos)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_min_pos", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$acc_per_sec_min_pos, 1.5)
outliers[order(outliers$acc_per_sec_min_pos, decreasing = T),]
outliers[order(outliers$acc_per_sec_min_pos),]
```






### missings_distance {.tabset .tabset-fade .tabset-pills}
**Расстояние промахов от центра мишеней, пикс.**

#### Descriptive stat
```{r}
quantile(df$missings_distance, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_distance))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_distance"
plot_list <- draw_plots_continuous(df, var = df$missings_distance, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_distance);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_distance, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_distance)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_distance", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_distance, 1.5)
outliers[order(outliers$missings_distance, decreasing = T),]
outliers[order(outliers$missings_distance),]
```






### missings_mean {.tabset .tabset-fade .tabset-pills}
**Среднее расстояние промахов от центра мишеней, пикс.**

#### Descriptive stat
```{r}
quantile(df$missings_mean, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_mean))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_mean"
plot_list <- draw_plots_continuous(df, var = df$missings_mean, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_mean, 1.5)
outliers[order(outliers$missings_mean, decreasing = T),]
outliers[order(outliers$missings_mean),]
```






### missings_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_sd, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_sd))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_sd"
plot_list <- draw_plots_continuous(df, var = df$missings_sd, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_sd, 1.5)
outliers[order(outliers$missings_sd, decreasing = T),]
outliers[order(outliers$missings_sd),]
```






### missings_median {.tabset .tabset-fade .tabset-pills}
**Медиана расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_median, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_median))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_median"
plot_list <- draw_plots_continuous(df, var = df$missings_median, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_median, 1.5)
outliers[order(outliers$missings_median, decreasing = T),]
outliers[order(outliers$missings_median),]
```






### missings_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_trimmed, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_trimmed))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_trimmed"
plot_list <- draw_plots_continuous(df, var = df$missings_trimmed, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_trimmed, 1.5)
outliers[order(outliers$missings_trimmed, decreasing = T),]
outliers[order(outliers$missings_trimmed),]
```






### missings_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_mad, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_mad))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_mad"
plot_list <- draw_plots_continuous(df, var = df$missings_mad, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_mad, 1.5)
outliers[order(outliers$missings_mad, decreasing = T),]
outliers[order(outliers$missings_mad),]
```






### missings_min {.tabset .tabset-fade .tabset-pills}
**Минимальное расстояние промахов**

#### Descriptive stat
```{r}
quantile(df$missings_min, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_min))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_min"
plot_list <- draw_plots_continuous(df, var = df$missings_min, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_min, 1.5)
outliers[order(outliers$missings_min, decreasing = T),]
outliers[order(outliers$missings_min),]
```






### missings_max {.tabset .tabset-fade .tabset-pills}
**Максимальное расстояние промахов**

#### Descriptive stat
```{r}
quantile(df$missings_max, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_max))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_max"
plot_list <- draw_plots_continuous(df, var = df$missings_max, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_max, 1.5)
outliers[order(outliers$missings_max, decreasing = T),]
outliers[order(outliers$missings_max),]
```






### missings_range {.tabset .tabset-fade .tabset-pills}
**Размах расстояния промахов**

#### Descriptive stat
```{r}
quantile(df$missings_range, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_range))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_range"
plot_list <- draw_plots_continuous(df, var = df$missings_range, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_range, 1.5)
outliers[order(outliers$missings_range, decreasing = T),]
outliers[order(outliers$missings_range),]
```






### missings_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_skew, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_skew))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_skew"
plot_list <- draw_plots_continuous(df, var = df$missings_skew, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_skew, 1.5)
outliers[order(outliers$missings_skew, decreasing = T),]
outliers[order(outliers$missings_skew),]
```






### missings_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_kurtosis, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_kurtosis))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_kurtosis"
plot_list <- draw_plots_continuous(df, var = df$missings_kurtosis, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_kurtosis, 1.5)
outliers[order(outliers$missings_kurtosis, decreasing = T),]
outliers[order(outliers$missings_kurtosis),]
```






### missings_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_se, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_se))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_se"
plot_list <- draw_plots_continuous(df, var = df$missings_se, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_se, 1.5)
outliers[order(outliers$missings_se, decreasing = T),]
outliers[order(outliers$missings_se),]
```






### missings_Q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_Q0.25, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_Q0.25))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_Q0.25"
plot_list <- draw_plots_continuous(df, var = df$missings_Q0.25, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_Q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_Q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_Q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_Q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_Q0.25, 1.5)
outliers[order(outliers$missings_Q0.25, decreasing = T),]
outliers[order(outliers$missings_Q0.25),]
```






### missings_Q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль расстояний промахов**

#### Descriptive stat
```{r}
quantile(df$missings_Q0.75, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_Q0.75))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_Q0.75"
plot_list <- draw_plots_continuous(df, var = df$missings_Q0.75, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_Q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_Q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_Q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_Q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_Q0.75, 1.5)
outliers[order(outliers$missings_Q0.75, decreasing = T),]
outliers[order(outliers$missings_Q0.75),]
```






### missings {.tabset .tabset-fade .tabset-pills}
**Количество промахов, шт.**

#### Descriptive stat
```{r}
quantile(df$missings, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings"
plot_list <- draw_plots_continuous(df, var = df$missings, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings, 1.5)
outliers[order(outliers$missings, decreasing = T),]
outliers[order(outliers$missings),]
```






### hits {.tabset .tabset-fade .tabset-pills}
**Количество попаданий, шт.**

#### Descriptive stat
```{r}
quantile(df$hits, na.rm = T)
```

```{r}
data.frame(describeBy(df$hits))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "hits"
plot_list <- draw_plots_continuous(df, var = df$hits, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$hits);print(st)
check_normal_distribution(st)

ks <- ks.test(df$hits, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = hits)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - hits", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$hits, 1.5)
outliers[order(outliers$hits, decreasing = T),]
outliers[order(outliers$hits),]
```






### missings_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд расстояния промахов**

#### Descriptive stat
```{r}
quantile(df$missings_trend, na.rm = T)
```

```{r}
data.frame(describeBy(df$missings_trend))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_trend"
plot_list <- draw_plots_continuous(df, var = df$missings_trend, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$missings_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df$missings_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = missings_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$missings_trend, 1.5)
outliers[order(outliers$missings_trend, decreasing = T),]
outliers[order(outliers$missings_trend),]
```






### accuracy_rate {.tabset .tabset-fade .tabset-pills}
**Точность, %**

#### Descriptive stat
```{r}
quantile(df$accuracy_rate, na.rm = T)
```

```{r}
data.frame(describeBy(df$accuracy_rate))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "accuracy_rate"
plot_list <- draw_plots_continuous(df, var = df$accuracy_rate, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$accuracy_rate);print(st)
check_normal_distribution(st)

ks <- ks.test(df$accuracy_rate, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = accuracy_rate)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - accuracy_rate", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$accuracy_rate, 1.5)
outliers[order(outliers$accuracy_rate, decreasing = T),]
outliers[order(outliers$accuracy_rate),]
```






### combo_hits {.tabset .tabset-fade .tabset-pills}
**Комбо попаданий**

#### Descriptive stat
```{r}
quantile(df$combo_hits, na.rm = T)
```

```{r}
data.frame(describeBy(df$combo_hits))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "combo_hits"
plot_list <- draw_plots_continuous(df, var = df$combo_hits, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$combo_hits);print(st)
check_normal_distribution(st)

ks <- ks.test(df$combo_hits, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = combo_hits)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - combo_hits", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$combo_hits, 1.5)
outliers[order(outliers$combo_hits, decreasing = T),]
outliers[order(outliers$combo_hits),]
```






### combo_missings {.tabset .tabset-fade .tabset-pills}
**Комбо пропахов**

#### Descriptive stat
```{r}
quantile(df$combo_missings, na.rm = T)
```

```{r}
data.frame(describeBy(df$combo_missings))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "combo_missings"
plot_list <- draw_plots_continuous(df, var = df$combo_missings, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$combo_missings);print(st)
check_normal_distribution(st)

ks <- ks.test(df$combo_missings, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = combo_missings)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - combo_missings", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$combo_missings, 1.5)
outliers[order(outliers$combo_missings, decreasing = T),]
outliers[order(outliers$combo_missings),]
```






### corr_sat {.tabset .tabset-fade .tabset-pills}
**Коэффициент корреляции скорости и точности**

#### Descriptive stat
```{r}
quantile(df$corr_sat, na.rm = T)
```

```{r}
data.frame(describeBy(df$corr_sat))
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "corr_sat"
plot_list <- draw_plots_continuous(df, var = df$corr_sat, title = plot_title, filename = NULL, binwidth = NULL, bins = 15, alpha = 0.5, angle = 0)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df$corr_sat);print(st)
check_normal_distribution(st)

ks <- ks.test(df$corr_sat, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df, aes(sample = corr_sat)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - corr_sat", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Outliers
```{r echo=FALSE}
outliers <- get_outliers(df, df$corr_sat, 1.5)
outliers[order(outliers$corr_sat, decreasing = T),]
outliers[order(outliers$corr_sat),]
```







