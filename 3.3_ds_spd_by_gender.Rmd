---
title: "Описательная статистика"
author: "SVA"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document:
    css: "styles.css"
    code_folding: hide
    df_print: paged
    toc: yes
    toc_depth: 4
    toc_float:
      collapsed: no
  word_document:
    toc: yes
    toc_depth: '4'
editor_options:
  chunk_output_type: inline
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  include = TRUE,
  message = FALSE, 
  warning = FALSE
)

rm(list=ls())
source("functions.R")
library("ARTool")
```
<script>
function addClassToTitle(containerId, className) {
  // Находим контейнер по id
  var container = document.getElementById(containerId);
  if (!container) {
    console.warn('Контейнер с id "' + containerId + '" не найден.');
    return;
  }

  // Добавляем класс к найденному h3
  container.classList.add(className);
}
</script>

```{r}
# Набор данных: Точность
df.acc <- read.csv("./data/accuracy_features.csv")

df.acc$acc_max[is.infinite(df.acc$acc_max)] <- NA
df.acc$acc_mean[is.infinite(df.acc$acc_mean)] <- NA
df.acc$acc_range[is.infinite(df.acc$acc_range)] <- NA

df.acc$dec_max[is.infinite(df.acc$dec_max)] <- NA
df.acc$dec_mean[is.infinite(df.acc$dec_mean)] <- NA
df.acc$dec_range[is.infinite(df.acc$dec_range)] <- NA

df.acc$X <- NULL
df.acc$stage <- "accuracy"

names(df.acc) <- tolower(names(df.acc))
```

```{r}
# Набор данных: Скорость
df.spd <- read.csv("./data/speed_features.csv")

df.spd$acc_max[is.infinite(df.spd$acc_max)] <- NA
df.spd$acc_mean[is.infinite(df.spd$acc_mean)] <- NA
df.spd$acc_range[is.infinite(df.spd$acc_range)] <- NA

df.spd$dec_max[is.infinite(df.spd$dec_max)] <- NA
df.spd$dec_mean[is.infinite(df.spd$dec_mean)] <- NA
df.spd$dec_range[is.infinite(df.spd$dec_range)] <- NA
df.spd$dec_min[is.infinite(df.spd$dec_min)] <- NA

df.spd$X <- NULL
df.spd$stage <- "speed"

names(df.spd) <- tolower(names(df.spd))
```

```{r}
## Данные тестов психодиагностики
sp_results <- read_xlsx("../../Все тесты/data/results/sp_results_impersonal.xlsx")

df.acc <- merge(df.acc, select(sp_results, userid, gender), by.x = "user_id", by.y = "userid", all.x = T)
df.acc <- select(df.acc, 1:4, gender, everything())
df.acc <- df.acc[!is.na(df.acc$gender),]
df.acc$gender <- factor(df.acc$gender, labels = c("F", "M"))

df.spd <- merge(df.spd, select(sp_results, userid, gender), by.x = "user_id", by.y = "userid", all.x = T)
df.spd <- select(df.spd, 1:4, gender, everything())
df.spd <- df.spd[!is.na(df.spd$gender),]
df.spd$gender <- factor(df.spd$gender, labels = c("F", "M"))
```

```{r}
## Кликеры
clickers.acc <- df.acc[df.acc$clicked_through_targets > 5 & df.acc$combo_clicked_through_targets > 5, ]
clickers.spd <- df.spd[df.spd$clicked_through_targets > 5 & df.spd$combo_clicked_through_targets > 5, ]
clickers <- unique(c(clickers.acc$user_id, clickers.spd$user_id))

df.acc <- df.acc[!df.acc$user_id %in% clickers, ]
df.spd <- df.spd[!df.spd$user_id %in% clickers, ]

save(clickers.acc, file = "./data/envs/1_desc_stat/clickers.acc.RData")
save(clickers.spd, file = "./data/envs/1_desc_stat/clickers.spd.RData")
```


```{r}
## outliers
## Выбросы найденные вручную с помощью кластеризации
outliers.acc <- df.acc[df.acc$user_id %in% c(1264, 2700, 1121, 1915, 2045, 2637, 1359, 1717, 1855, 2358, 2676, 1182, 1747, 1981, 2992, 2633), ]
outliers.spd <- df.spd[df.spd$user_id %in% c(), ]
excluded_outliers <- unique(c(outliers.acc$user_id, outliers.spd$user_id))

df.acc <- df.acc[!df.acc$user_id %in% excluded_outliers,]
df.spd <- df.spd[!df.spd$user_id %in% excluded_outliers,]

save(outliers.acc, file = "./data/envs/1_desc_stat/outliers.acc.RData")
save(outliers.spd, file = "./data/envs/1_desc_stat/outliers.spd.RData")

```


```{r}
# # Очень продолжительное выполнение теста
# longduration.acc <- df.acc[df.acc$time_total %in% boxplot.stats(df.acc$time_total, coef = 1)$out, ]
# longduration.spd <- df.spd[df.spd$time_total %in% boxplot.stats(df.spd$time_total, coef = 1)$out, ]
# longduration <- unique(c(longduration.acc$user_id, longduration.spd$user_id))
# 
# 
# df.acc <- df.acc[!df.acc$user_id %in% longduration, ]
# df.spd <- df.spd[!df.spd$user_id %in% longduration, ]

# save(longduration.acc, file = "./data/envs/1_desc_stat/longduration.acc.RData")
# save(longduration.spd, file = "./data/envs/1_desc_stat/longduration.spd.RData")
```


```{r}
# # Парадоксальные
# paradox.acc <- df.acc[df.acc$corr_sat < 0, ]
# paradox.spd <- df.spd[df.spd$corr_sat < 0, ]
# paradox <- unique(c(paradox.acc$user_id, paradox.spd$user_id))
# 
# 
# df.acc <- df.acc[!df.acc$user_id %in% paradox, ]
# df.spd <- df.spd[!df.spd$user_id %in% paradox, ]

# save(paradox.acc, file = "./data/envs/1_desc_stat/paradox.acc.RData")
# save(paradox.spd, file = "./data/envs/1_desc_stat/paradox.spd.RData")
```



```{r}
## Остановки во время выполнения теста (можно попробовать восстановить)
long_stops.acc <- df.acc[df.acc$time_max >= 10, ]
long_stops.spd <- df.spd[df.spd$time_max >= 10, ]
long_stops <- unique(c(long_stops.acc$user_id, long_stops.spd$user_id))

df.acc <- df.acc[!df.acc$user_id %in% long_stops, ]
df.spd <- df.spd[!df.spd$user_id %in% long_stops, ]
```

```{r}
## Длинный формат данных
df <- rbind(df.acc, df.spd)

df$gender <- factor(df$gender, labels = c("F", "M"))
df$stage <- factor(df$stage, levels = c("accuracy", "speed"))
df$user_id <- factor(df$user_id)
```


```{r}
save(df, file = "./data/envs/1_desc_stat/df.RData")
save(df.acc, file = "./data/envs/1_desc_stat/df.acc.RData")
save(df.spd, file = "./data/envs/1_desc_stat/df.spd.RData")
save(sp_results, file = "./data/envs/1_desc_stat/sp_results.RData")
```

```{r}
sign_differences <- vector()
outliers_list <- list()
```






# Описательные статистики


















### ratio {.tabset .tabset-fade .tabset-pills}
**ratio**

#### Tables

Распределение по переменной "ratio"

```{r}
tab <- table(df.spd$ratio)

data.frame(
  level = names(tab),
  frequency = as.vector(tab),
  percent = as.vector(round(prop.table(tab) * 100, 2))
)
```







### gender {.tabset .tabset-fade .tabset-pills}
**Пол респондента**

#### Tables

Распределение по переменной "gender"

```{r}
tab <- table(df.spd$gender)

data.frame(
  level = names(tab),
  frequency = as.vector(tab),
  percent = as.vector(round(prop.table(tab) * 100, 2))
)
```






### time_total {.tabset .tabset-fade .tabset-pills}
**Общее время выполнения**

#### Descriptive stat

```{r}
describeBy(time_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_total,  df.spd$gender, group_lab = "gender", x_lab = "Общее время выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_total[df.spd$gender == "M"], df.spd$time_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_total[df.spd$gender == "M"], df.spd$time_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_mean {.tabset .tabset-fade .tabset-pills}
**Средняя гармоническая скорость**

#### Descriptive stat

```{r}
describeBy(velocity_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_mean,  df.spd$gender, group_lab = "gender", x_lab = "Средняя гармоническая скорость", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_mean[df.spd$gender == "M"], df.spd$velocity_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_mean[df.spd$gender == "M"], df.spd$velocity_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_distance {.tabset .tabset-fade .tabset-pills}
**Расстояние промахов от центра мишеней, пикс.**

#### Descriptive stat

```{r}
describeBy(missings_distance ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_distance"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_distance,  df.spd$gender, group_lab = "gender", x_lab = "Расстояние промахов от центра мишеней, пикс.", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_distance);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_distance, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_distance)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_distance", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_distance[df.spd$gender == "M"], df.spd$missings_distance[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_distance[df.spd$gender == "M"], df.spd$missings_distance[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_distance")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_distance', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_distance, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_total {.tabset .tabset-fade .tabset-pills}
**Общая амплитуда**

#### Descriptive stat

```{r}
describeBy(distance_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_total,  df.spd$gender, group_lab = "gender", x_lab = "Общая амплитуда", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_total[df.spd$gender == "M"], df.spd$distance_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_total[df.spd$gender == "M"], df.spd$distance_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_total {.tabset .tabset-fade .tabset-pills}
**Общее время пауз**

#### Descriptive stat

```{r}
describeBy(pause_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_total,  df.spd$gender, group_lab = "gender", x_lab = "Общее время пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_total[df.spd$gender == "M"], df.spd$pause_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_total[df.spd$gender == "M"], df.spd$pause_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_total {.tabset .tabset-fade .tabset-pills}
**Общее время выполнения**

#### Descriptive stat

```{r}
describeBy(time_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_total,  df.spd$gender, group_lab = "gender", x_lab = "Общее время выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_total[df.spd$gender == "M"], df.spd$time_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_total[df.spd$gender == "M"], df.spd$time_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_mean {.tabset .tabset-fade .tabset-pills}
**Среднее время на клик**

#### Descriptive stat

```{r}
describeBy(time_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_mean,  df.spd$gender, group_lab = "gender", x_lab = "Среднее время на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_mean[df.spd$gender == "M"], df.spd$time_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_mean[df.spd$gender == "M"], df.spd$time_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение времени на клик**

#### Descriptive stat

```{r}
describeBy(time_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение времени на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_sd[df.spd$gender == "M"], df.spd$time_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_sd[df.spd$gender == "M"], df.spd$time_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_median {.tabset .tabset-fade .tabset-pills}
**Медианное время на клик**

#### Descriptive stat

```{r}
describeBy(time_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианное время на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_median[df.spd$gender == "M"], df.spd$time_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_median[df.spd$gender == "M"], df.spd$time_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее времени на клик**

#### Descriptive stat

```{r}
describeBy(time_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее времени на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_trimmed[df.spd$gender == "M"], df.spd$time_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_trimmed[df.spd$gender == "M"], df.spd$time_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение времени на клик**

#### Descriptive stat

```{r}
describeBy(time_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее Абсолютное Отклонение времени на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_mad[df.spd$gender == "M"], df.spd$time_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_mad[df.spd$gender == "M"], df.spd$time_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_min {.tabset .tabset-fade .tabset-pills}
**Минимальное время клика**

#### Descriptive stat

```{r}
describeBy(time_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное время клика", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_min[df.spd$gender == "M"], df.spd$time_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_min[df.spd$gender == "M"], df.spd$time_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_max {.tabset .tabset-fade .tabset-pills}
**Максимальное время клика**

#### Descriptive stat

```{r}
describeBy(time_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное время клика", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_max[df.spd$gender == "M"], df.spd$time_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_max[df.spd$gender == "M"], df.spd$time_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_range {.tabset .tabset-fade .tabset-pills}
**Размах времени на клик**

#### Descriptive stat

```{r}
describeBy(time_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах времени на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_range[df.spd$gender == "M"], df.spd$time_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_range[df.spd$gender == "M"], df.spd$time_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_skew[df.spd$gender == "M"], df.spd$time_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_skew[df.spd$gender == "M"], df.spd$time_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_kurtosis[df.spd$gender == "M"], df.spd$time_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_kurtosis[df.spd$gender == "M"], df.spd$time_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка среднего времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка среднего времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_se[df.spd$gender == "M"], df.spd$time_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_se[df.spd$gender == "M"], df.spd$time_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_q0.25[df.spd$gender == "M"], df.spd$time_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_q0.25[df.spd$gender == "M"], df.spd$time_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_q0.75[df.spd$gender == "M"], df.spd$time_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_q0.75[df.spd$gender == "M"], df.spd$time_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### time_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд времени выполнения**

#### Descriptive stat

```{r}
describeBy(time_trend ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "time_trend"
plot_list <- draw_plots_continuous_by_group(df.spd, var = time_trend,  df.spd$gender, group_lab = "gender", x_lab = "Линейный тренд времени выполнения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$time_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$time_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = time_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - time_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$time_trend[df.spd$gender == "M"], df.spd$time_trend[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$time_trend[df.spd$gender == "M"], df.spd$time_trend[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "time_trend")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('time_trend', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=time_trend, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### fitts_a {.tabset .tabset-fade .tabset-pills}
**Коэффициент a в формуле Фиттса**

#### Descriptive stat

```{r}
describeBy(fitts_a ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "fitts_a"
plot_list <- draw_plots_continuous_by_group(df.spd, var = fitts_a,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент a в формуле Фиттса", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$fitts_a);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$fitts_a, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = fitts_a)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - fitts_a", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$fitts_a[df.spd$gender == "M"], df.spd$fitts_a[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$fitts_a[df.spd$gender == "M"], df.spd$fitts_a[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "fitts_a")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('fitts_a', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=fitts_a, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### fitts_b {.tabset .tabset-fade .tabset-pills}
**Коэффициент b в формуле Фиттса**

#### Descriptive stat

```{r}
describeBy(fitts_b ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "fitts_b"
plot_list <- draw_plots_continuous_by_group(df.spd, var = fitts_b,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент b в формуле Фиттса", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$fitts_b);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$fitts_b, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = fitts_b)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - fitts_b", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$fitts_b[df.spd$gender == "M"], df.spd$fitts_b[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$fitts_b[df.spd$gender == "M"], df.spd$fitts_b[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "fitts_b")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('fitts_b', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=fitts_b, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_total {.tabset .tabset-fade .tabset-pills}
**Общая амплитуда**

#### Descriptive stat

```{r}
describeBy(distance_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_total,  df.spd$gender, group_lab = "gender", x_lab = "Общая амплитуда", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_total[df.spd$gender == "M"], df.spd$distance_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_total[df.spd$gender == "M"], df.spd$distance_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_mean {.tabset .tabset-fade .tabset-pills}
**Средняя амплитуда на клик**

#### Descriptive stat

```{r}
describeBy(distance_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_mean,  df.spd$gender, group_lab = "gender", x_lab = "Средняя амплитуда на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_mean[df.spd$gender == "M"], df.spd$distance_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_mean[df.spd$gender == "M"], df.spd$distance_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение амплитуда на клик**

#### Descriptive stat

```{r}
describeBy(distance_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение амплитуда на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_sd[df.spd$gender == "M"], df.spd$distance_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_sd[df.spd$gender == "M"], df.spd$distance_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_median {.tabset .tabset-fade .tabset-pills}
**Медианная амплитуда на клик**

#### Descriptive stat

```{r}
describeBy(distance_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианная амплитуда на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_median[df.spd$gender == "M"], df.spd$distance_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_median[df.spd$gender == "M"], df.spd$distance_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее амплитуды на клик**

#### Descriptive stat

```{r}
describeBy(distance_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее амплитуды на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_trimmed[df.spd$gender == "M"], df.spd$distance_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_trimmed[df.spd$gender == "M"], df.spd$distance_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение амплитуды на клик**

#### Descriptive stat

```{r}
describeBy(distance_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее Абсолютное Отклонение амплитуды на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_mad[df.spd$gender == "M"], df.spd$distance_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_mad[df.spd$gender == "M"], df.spd$distance_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_min {.tabset .tabset-fade .tabset-pills}
**Минимальная амплитуда клика**

#### Descriptive stat

```{r}
describeBy(distance_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальная амплитуда клика", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_min[df.spd$gender == "M"], df.spd$distance_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_min[df.spd$gender == "M"], df.spd$distance_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_max {.tabset .tabset-fade .tabset-pills}
**Максимальная амплитуда клика**

#### Descriptive stat

```{r}
describeBy(distance_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальная амплитуда клика", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_max[df.spd$gender == "M"], df.spd$distance_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_max[df.spd$gender == "M"], df.spd$distance_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_range {.tabset .tabset-fade .tabset-pills}
**Размах амплитуды на клик**

#### Descriptive stat

```{r}
describeBy(distance_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах амплитуды на клик", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_range[df.spd$gender == "M"], df.spd$distance_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_range[df.spd$gender == "M"], df.spd$distance_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_skew[df.spd$gender == "M"], df.spd$distance_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_skew[df.spd$gender == "M"], df.spd$distance_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_kurtosis[df.spd$gender == "M"], df.spd$distance_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_kurtosis[df.spd$gender == "M"], df.spd$distance_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка среднего амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка среднего амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_se[df.spd$gender == "M"], df.spd$distance_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_se[df.spd$gender == "M"], df.spd$distance_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_q0.25[df.spd$gender == "M"], df.spd$distance_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_q0.25[df.spd$gender == "M"], df.spd$distance_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_q0.75[df.spd$gender == "M"], df.spd$distance_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_q0.75[df.spd$gender == "M"], df.spd$distance_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### distance_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд амплитуды**

#### Descriptive stat

```{r}
describeBy(distance_trend ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "distance_trend"
plot_list <- draw_plots_continuous_by_group(df.spd, var = distance_trend,  df.spd$gender, group_lab = "gender", x_lab = "Линейный тренд амплитуды", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$distance_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$distance_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = distance_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - distance_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$distance_trend[df.spd$gender == "M"], df.spd$distance_trend[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$distance_trend[df.spd$gender == "M"], df.spd$distance_trend[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "distance_trend")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('distance_trend', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=distance_trend, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_total {.tabset .tabset-fade .tabset-pills}
**Общее время пауз**

#### Descriptive stat

```{r}
describeBy(pause_total ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_total"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_total,  df.spd$gender, group_lab = "gender", x_lab = "Общее время пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_total);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_total, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_total)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_total", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_total[df.spd$gender == "M"], df.spd$pause_total[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_total[df.spd$gender == "M"], df.spd$pause_total[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_total")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_total', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_total, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_number {.tabset .tabset-fade .tabset-pills}
**Количество пауз**

#### Descriptive stat

```{r}
describeBy(pause_number ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_number"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_number,  df.spd$gender, group_lab = "gender", x_lab = "Количество пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_number);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_number, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_number)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_number", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_number[df.spd$gender == "M"], df.spd$pause_number[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_number[df.spd$gender == "M"], df.spd$pause_number[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_number")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_number', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_number, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_mean {.tabset .tabset-fade .tabset-pills}
**Среднее пауза**

#### Descriptive stat

```{r}
describeBy(pause_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_mean,  df.spd$gender, group_lab = "gender", x_lab = "Среднее пауза", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_mean[df.spd$gender == "M"], df.spd$pause_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_mean[df.spd$gender == "M"], df.spd$pause_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение пауз**

#### Descriptive stat

```{r}
describeBy(pause_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_sd[df.spd$gender == "M"], df.spd$pause_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_sd[df.spd$gender == "M"], df.spd$pause_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_median {.tabset .tabset-fade .tabset-pills}
**Медианная пауза**

#### Descriptive stat

```{r}
describeBy(pause_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианная пауза", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_median[df.spd$gender == "M"], df.spd$pause_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_median[df.spd$gender == "M"], df.spd$pause_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее пауз**

#### Descriptive stat

```{r}
describeBy(pause_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_trimmed[df.spd$gender == "M"], df.spd$pause_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_trimmed[df.spd$gender == "M"], df.spd$pause_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_mad {.tabset .tabset-fade .tabset-pills}
**Среднее Абсолютное Отклонение пауз**

#### Descriptive stat

```{r}
describeBy(pause_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее Абсолютное Отклонение пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_mad[df.spd$gender == "M"], df.spd$pause_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_mad[df.spd$gender == "M"], df.spd$pause_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_min {.tabset .tabset-fade .tabset-pills}
**Минимальное время паузы**

#### Descriptive stat

```{r}
describeBy(pause_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное время паузы", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_min[df.spd$gender == "M"], df.spd$pause_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_min[df.spd$gender == "M"], df.spd$pause_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_max {.tabset .tabset-fade .tabset-pills}
**Максимальное время паузы**

#### Descriptive stat

```{r}
describeBy(pause_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное время паузы", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_max[df.spd$gender == "M"], df.spd$pause_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_max[df.spd$gender == "M"], df.spd$pause_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_range {.tabset .tabset-fade .tabset-pills}
**Размах пауз**

#### Descriptive stat

```{r}
describeBy(pause_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_range[df.spd$gender == "M"], df.spd$pause_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_range[df.spd$gender == "M"], df.spd$pause_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии пауз**

#### Descriptive stat

```{r}
describeBy(pause_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_skew[df.spd$gender == "M"], df.spd$pause_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_skew[df.spd$gender == "M"], df.spd$pause_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс пауз**

#### Descriptive stat

```{r}
describeBy(pause_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_kurtosis[df.spd$gender == "M"], df.spd$pause_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_kurtosis[df.spd$gender == "M"], df.spd$pause_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка пауз**

#### Descriptive stat

```{r}
describeBy(pause_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_se[df.spd$gender == "M"], df.spd$pause_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_se[df.spd$gender == "M"], df.spd$pause_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль пауз**

#### Descriptive stat

```{r}
describeBy(pause_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_q0.25[df.spd$gender == "M"], df.spd$pause_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_q0.25[df.spd$gender == "M"], df.spd$pause_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль пауз**

#### Descriptive stat

```{r}
describeBy(pause_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_q0.75[df.spd$gender == "M"], df.spd$pause_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_q0.75[df.spd$gender == "M"], df.spd$pause_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### pause_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд пауз**

#### Descriptive stat

```{r}
describeBy(pause_trend ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "pause_trend"
plot_list <- draw_plots_continuous_by_group(df.spd, var = pause_trend,  df.spd$gender, group_lab = "gender", x_lab = "Линейный тренд пауз", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$pause_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$pause_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = pause_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - pause_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$pause_trend[df.spd$gender == "M"], df.spd$pause_trend[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$pause_trend[df.spd$gender == "M"], df.spd$pause_trend[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "pause_trend")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('pause_trend', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=pause_trend, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### stops {.tabset .tabset-fade .tabset-pills}
**Количество остановок**

#### Descriptive stat

```{r}
describeBy(stops ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "stops"
plot_list <- draw_plots_continuous_by_group(df.spd, var = stops,  df.spd$gender, group_lab = "gender", x_lab = "Количество остановок", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$stops);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$stops, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = stops)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - stops", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$stops[df.spd$gender == "M"], df.spd$stops[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$stops[df.spd$gender == "M"], df.spd$stops[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "stops")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('stops', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=stops, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_mean {.tabset .tabset-fade .tabset-pills}
**Средняя гармоническая скорость**

#### Descriptive stat

```{r}
describeBy(velocity_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_mean,  df.spd$gender, group_lab = "gender", x_lab = "Средняя гармоническая скорость", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_mean[df.spd$gender == "M"], df.spd$velocity_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_mean[df.spd$gender == "M"], df.spd$velocity_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение скорости**

#### Descriptive stat

```{r}
describeBy(velocity_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_sd[df.spd$gender == "M"], df.spd$velocity_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_sd[df.spd$gender == "M"], df.spd$velocity_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение скорости**

#### Descriptive stat

```{r}
describeBy(velocity_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее абсолютное отклонение скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_mad[df.spd$gender == "M"], df.spd$velocity_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_mad[df.spd$gender == "M"], df.spd$velocity_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_min {.tabset .tabset-fade .tabset-pills}
**Минимальная скорость**

#### Descriptive stat

```{r}
describeBy(velocity_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальная скорость", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_min[df.spd$gender == "M"], df.spd$velocity_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_min[df.spd$gender == "M"], df.spd$velocity_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_max {.tabset .tabset-fade .tabset-pills}
**Максимальная скорость**

#### Descriptive stat

```{r}
describeBy(velocity_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальная скорость", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_max[df.spd$gender == "M"], df.spd$velocity_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_max[df.spd$gender == "M"], df.spd$velocity_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_range {.tabset .tabset-fade .tabset-pills}
**Размах скорости**

#### Descriptive stat

```{r}
describeBy(velocity_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_range[df.spd$gender == "M"], df.spd$velocity_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_range[df.spd$gender == "M"], df.spd$velocity_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии скорости**

#### Descriptive stat

```{r}
describeBy(velocity_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_skew[df.spd$gender == "M"], df.spd$velocity_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_skew[df.spd$gender == "M"], df.spd$velocity_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс скорости**

#### Descriptive stat

```{r}
describeBy(velocity_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_kurtosis[df.spd$gender == "M"], df.spd$velocity_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_kurtosis[df.spd$gender == "M"], df.spd$velocity_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка скорости**

#### Descriptive stat

```{r}
describeBy(velocity_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_se[df.spd$gender == "M"], df.spd$velocity_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_se[df.spd$gender == "M"], df.spd$velocity_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль скорости**

#### Descriptive stat

```{r}
describeBy(velocity_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_q0.25[df.spd$gender == "M"], df.spd$velocity_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_q0.25[df.spd$gender == "M"], df.spd$velocity_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль скорости**

#### Descriptive stat

```{r}
describeBy(velocity_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_q0.75[df.spd$gender == "M"], df.spd$velocity_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_q0.75[df.spd$gender == "M"], df.spd$velocity_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### velocity_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд скорости**

#### Descriptive stat

```{r}
describeBy(velocity_trend ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "velocity_trend"
plot_list <- draw_plots_continuous_by_group(df.spd, var = velocity_trend,  df.spd$gender, group_lab = "gender", x_lab = "Линейный тренд скорости", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$velocity_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$velocity_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = velocity_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - velocity_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$velocity_trend[df.spd$gender == "M"], df.spd$velocity_trend[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$velocity_trend[df.spd$gender == "M"], df.spd$velocity_trend[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "velocity_trend")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('velocity_trend', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=velocity_trend, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_n {.tabset .tabset-fade .tabset-pills}
**Количество ускорений**

#### Descriptive stat

```{r}
describeBy(acc_n ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_n"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_n,  df.spd$gender, group_lab = "gender", x_lab = "Количество ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_n);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_n, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_n)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_n", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_n[df.spd$gender == "M"], df.spd$acc_n[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_n[df.spd$gender == "M"], df.spd$acc_n[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_n")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_n', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_n, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```











### acc_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение ускорений**

#### Descriptive stat

```{r}
describeBy(acc_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_sd[df.spd$gender == "M"], df.spd$acc_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_sd[df.spd$gender == "M"], df.spd$acc_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_median {.tabset .tabset-fade .tabset-pills}
**Медианная ускорение**

#### Descriptive stat

```{r}
describeBy(acc_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианная ускорение", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_median[df.spd$gender == "M"], df.spd$acc_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_median[df.spd$gender == "M"], df.spd$acc_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее ускорения**

#### Descriptive stat

```{r}
describeBy(acc_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее ускорения", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_trimmed[df.spd$gender == "M"], df.spd$acc_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_trimmed[df.spd$gender == "M"], df.spd$acc_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение ускорений**

#### Descriptive stat

```{r}
describeBy(acc_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее абсолютное отклонение ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_mad[df.spd$gender == "M"], df.spd$acc_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_mad[df.spd$gender == "M"], df.spd$acc_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_min {.tabset .tabset-fade .tabset-pills}
**Минимальное ускорение**

#### Descriptive stat

```{r}
describeBy(acc_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное ускорение", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_min[df.spd$gender == "M"], df.spd$acc_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_min[df.spd$gender == "M"], df.spd$acc_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_max {.tabset .tabset-fade .tabset-pills}
**Максимальное ускорение**

#### Descriptive stat

```{r}
describeBy(acc_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное ускорение", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_max[df.spd$gender == "M"], df.spd$acc_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_max[df.spd$gender == "M"], df.spd$acc_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_range {.tabset .tabset-fade .tabset-pills}
**Размах ускорений**

#### Descriptive stat

```{r}
describeBy(acc_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_range[df.spd$gender == "M"], df.spd$acc_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_range[df.spd$gender == "M"], df.spd$acc_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии ускорений**

#### Descriptive stat

```{r}
describeBy(acc_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_skew[df.spd$gender == "M"], df.spd$acc_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_skew[df.spd$gender == "M"], df.spd$acc_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс ускорений**

#### Descriptive stat

```{r}
describeBy(acc_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_kurtosis[df.spd$gender == "M"], df.spd$acc_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_kurtosis[df.spd$gender == "M"], df.spd$acc_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка ускорений**

#### Descriptive stat

```{r}
describeBy(acc_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_se[df.spd$gender == "M"], df.spd$acc_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_se[df.spd$gender == "M"], df.spd$acc_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль ускорений**

#### Descriptive stat

```{r}
describeBy(acc_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_q0.25[df.spd$gender == "M"], df.spd$acc_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_q0.25[df.spd$gender == "M"], df.spd$acc_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль ускорений**

#### Descriptive stat

```{r}
describeBy(acc_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_q0.75[df.spd$gender == "M"], df.spd$acc_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_q0.75[df.spd$gender == "M"], df.spd$acc_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### jerk_line {.tabset .tabset-fade .tabset-pills}
**Линия рывков**

#### Descriptive stat

```{r}
describeBy(jerk_line ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "jerk_line"
plot_list <- draw_plots_continuous_by_group(df.spd, var = jerk_line,  df.spd$gender, group_lab = "gender", x_lab = "Линия рывков", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$jerk_line);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$jerk_line, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = jerk_line)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - jerk_line", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$jerk_line[df.spd$gender == "M"], df.spd$jerk_line[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$jerk_line[df.spd$gender == "M"], df.spd$jerk_line[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "jerk_line")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('jerk_line', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=jerk_line, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### n_jerk {.tabset .tabset-fade .tabset-pills}
**Количество рывков**

#### Descriptive stat

```{r}
describeBy(n_jerk ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "n_jerk"
plot_list <- draw_plots_continuous_by_group(df.spd, var = n_jerk,  df.spd$gender, group_lab = "gender", x_lab = "Количество рывков", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$n_jerk);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$n_jerk, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = n_jerk)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - n_jerk", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$n_jerk[df.spd$gender == "M"], df.spd$n_jerk[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$n_jerk[df.spd$gender == "M"], df.spd$n_jerk[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "n_jerk")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('n_jerk', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=n_jerk, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_n {.tabset .tabset-fade .tabset-pills}
**Количество замедлений**

#### Descriptive stat

```{r}
describeBy(dec_n ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_n"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_n,  df.spd$gender, group_lab = "gender", x_lab = "Количество замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_n);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_n, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_n)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_n", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_n[df.spd$gender == "M"], df.spd$dec_n[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_n[df.spd$gender == "M"], df.spd$dec_n[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_n")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_n', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_n, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_mean {.tabset .tabset-fade .tabset-pills}
**Среднее замедление**

#### Descriptive stat

```{r}
describeBy(dec_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_mean,  df.spd$gender, group_lab = "gender", x_lab = "Среднее замедление", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_mean[df.spd$gender == "M"], df.spd$dec_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_mean[df.spd$gender == "M"], df.spd$dec_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение замедлений**

#### Descriptive stat

```{r}
describeBy(dec_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_sd[df.spd$gender == "M"], df.spd$dec_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_sd[df.spd$gender == "M"], df.spd$dec_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_median {.tabset .tabset-fade .tabset-pills}
**Медианная замедлений**

#### Descriptive stat

```{r}
describeBy(dec_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианная замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_median[df.spd$gender == "M"], df.spd$dec_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_median[df.spd$gender == "M"], df.spd$dec_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее замедлений**

#### Descriptive stat

```{r}
describeBy(dec_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_trimmed[df.spd$gender == "M"], df.spd$dec_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_trimmed[df.spd$gender == "M"], df.spd$dec_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение замедлений**

#### Descriptive stat

```{r}
describeBy(dec_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее абсолютное отклонение замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_mad[df.spd$gender == "M"], df.spd$dec_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_mad[df.spd$gender == "M"], df.spd$dec_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_min {.tabset .tabset-fade .tabset-pills}
**Минимальное замедление**

#### Descriptive stat

```{r}
describeBy(dec_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное замедление", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_min[df.spd$gender == "M"], df.spd$dec_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_min[df.spd$gender == "M"], df.spd$dec_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_max {.tabset .tabset-fade .tabset-pills}
**Максимальное замедление**

#### Descriptive stat

```{r}
describeBy(dec_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное замедление", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_max[df.spd$gender == "M"], df.spd$dec_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_max[df.spd$gender == "M"], df.spd$dec_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_range {.tabset .tabset-fade .tabset-pills}
**Размах замедлений**

#### Descriptive stat

```{r}
describeBy(dec_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_range[df.spd$gender == "M"], df.spd$dec_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_range[df.spd$gender == "M"], df.spd$dec_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии замедлений**

#### Descriptive stat

```{r}
describeBy(dec_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_skew[df.spd$gender == "M"], df.spd$dec_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_skew[df.spd$gender == "M"], df.spd$dec_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс замедлений**

#### Descriptive stat

```{r}
describeBy(dec_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_kurtosis[df.spd$gender == "M"], df.spd$dec_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_kurtosis[df.spd$gender == "M"], df.spd$dec_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка замедлений**

#### Descriptive stat

```{r}
describeBy(dec_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_se[df.spd$gender == "M"], df.spd$dec_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_se[df.spd$gender == "M"], df.spd$dec_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль замедлений**

#### Descriptive stat

```{r}
describeBy(dec_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_q0.25[df.spd$gender == "M"], df.spd$dec_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_q0.25[df.spd$gender == "M"], df.spd$dec_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### dec_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль замедлений**

#### Descriptive stat

```{r}
describeBy(dec_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "dec_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = dec_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль замедлений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$dec_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$dec_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = dec_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - dec_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$dec_q0.75[df.spd$gender == "M"], df.spd$dec_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$dec_q0.75[df.spd$gender == "M"], df.spd$dec_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "dec_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('dec_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=dec_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### braking_line {.tabset .tabset-fade .tabset-pills}
**Линия резких торможений**

#### Descriptive stat

```{r}
describeBy(braking_line ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "braking_line"
plot_list <- draw_plots_continuous_by_group(df.spd, var = braking_line,  df.spd$gender, group_lab = "gender", x_lab = "Линия резких торможений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$braking_line);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$braking_line, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = braking_line)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - braking_line", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$braking_line[df.spd$gender == "M"], df.spd$braking_line[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$braking_line[df.spd$gender == "M"], df.spd$braking_line[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "braking_line")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('braking_line', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=braking_line, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### n_braking {.tabset .tabset-fade .tabset-pills}
**Количество резких торможений**

#### Descriptive stat

```{r}
describeBy(n_braking ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "n_braking"
plot_list <- draw_plots_continuous_by_group(df.spd, var = n_braking,  df.spd$gender, group_lab = "gender", x_lab = "Количество резких торможений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$n_braking);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$n_braking, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = n_braking)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - n_braking", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$n_braking[df.spd$gender == "M"], df.spd$n_braking[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$n_braking[df.spd$gender == "M"], df.spd$n_braking[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "n_braking")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('n_braking', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=n_braking, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_mean {.tabset .tabset-fade .tabset-pills}
**Среднее количество ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_mean,  df.spd$gender, group_lab = "gender", x_lab = "Среднее количество ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_mean[df.spd$gender == "M"], df.spd$acc_per_sec_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_mean[df.spd$gender == "M"], df.spd$acc_per_sec_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение количества ускорений за секунду **

#### Descriptive stat

```{r}
describeBy(acc_per_sec_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение количества ускорений за секунду ", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_sd[df.spd$gender == "M"], df.spd$acc_per_sec_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_sd[df.spd$gender == "M"], df.spd$acc_per_sec_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_median {.tabset .tabset-fade .tabset-pills}
**Медианное количество ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_median,  df.spd$gender, group_lab = "gender", x_lab = "Медианное количество ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_median[df.spd$gender == "M"], df.spd$acc_per_sec_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_median[df.spd$gender == "M"], df.spd$acc_per_sec_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_trimmed[df.spd$gender == "M"], df.spd$acc_per_sec_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_trimmed[df.spd$gender == "M"], df.spd$acc_per_sec_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее абсолютное отклонение количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_mad[df.spd$gender == "M"], df.spd$acc_per_sec_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_mad[df.spd$gender == "M"], df.spd$acc_per_sec_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_min {.tabset .tabset-fade .tabset-pills}
**Минимальное количество ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное количество ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_min[df.spd$gender == "M"], df.spd$acc_per_sec_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_min[df.spd$gender == "M"], df.spd$acc_per_sec_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_max {.tabset .tabset-fade .tabset-pills}
**Максимальное количество ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное количество ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_max[df.spd$gender == "M"], df.spd$acc_per_sec_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_max[df.spd$gender == "M"], df.spd$acc_per_sec_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_range {.tabset .tabset-fade .tabset-pills}
**Размах количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_range[df.spd$gender == "M"], df.spd$acc_per_sec_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_range[df.spd$gender == "M"], df.spd$acc_per_sec_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_skew[df.spd$gender == "M"], df.spd$acc_per_sec_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_skew[df.spd$gender == "M"], df.spd$acc_per_sec_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_kurtosis[df.spd$gender == "M"], df.spd$acc_per_sec_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_kurtosis[df.spd$gender == "M"], df.spd$acc_per_sec_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_se[df.spd$gender == "M"], df.spd$acc_per_sec_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_se[df.spd$gender == "M"], df.spd$acc_per_sec_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_q0.25[df.spd$gender == "M"], df.spd$acc_per_sec_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_q0.25[df.spd$gender == "M"], df.spd$acc_per_sec_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль количества ускорений за секунду**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль количества ускорений за секунду", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_q0.75[df.spd$gender == "M"], df.spd$acc_per_sec_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_q0.75[df.spd$gender == "M"], df.spd$acc_per_sec_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_max_pos {.tabset .tabset-fade .tabset-pills}
**Позиция максимального количества ускорений**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_max_pos ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_max_pos"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_max_pos,  df.spd$gender, group_lab = "gender", x_lab = "Позиция максимального количества ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_max_pos);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_max_pos, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_max_pos)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_max_pos", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_max_pos[df.spd$gender == "M"], df.spd$acc_per_sec_max_pos[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_max_pos[df.spd$gender == "M"], df.spd$acc_per_sec_max_pos[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_max_pos")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_max_pos', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_max_pos, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### acc_per_sec_min_pos {.tabset .tabset-fade .tabset-pills}
**Позиция минимального количества ускорений**

#### Descriptive stat

```{r}
describeBy(acc_per_sec_min_pos ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "acc_per_sec_min_pos"
plot_list <- draw_plots_continuous_by_group(df.spd, var = acc_per_sec_min_pos,  df.spd$gender, group_lab = "gender", x_lab = "Позиция минимального количества ускорений", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$acc_per_sec_min_pos);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$acc_per_sec_min_pos, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = acc_per_sec_min_pos)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - acc_per_sec_min_pos", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$acc_per_sec_min_pos[df.spd$gender == "M"], df.spd$acc_per_sec_min_pos[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$acc_per_sec_min_pos[df.spd$gender == "M"], df.spd$acc_per_sec_min_pos[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "acc_per_sec_min_pos")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('acc_per_sec_min_pos', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=acc_per_sec_min_pos, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_distance {.tabset .tabset-fade .tabset-pills}
**Расстояние промахов от центра мишеней, пикс.**

#### Descriptive stat

```{r}
describeBy(missings_distance ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_distance"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_distance,  df.spd$gender, group_lab = "gender", x_lab = "Расстояние промахов от центра мишеней, пикс.", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_distance);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_distance, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_distance)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_distance", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_distance[df.spd$gender == "M"], df.spd$missings_distance[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_distance[df.spd$gender == "M"], df.spd$missings_distance[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_distance")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_distance', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_distance, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_mean {.tabset .tabset-fade .tabset-pills}
**Среднее расстояние промахов от центра мишеней, пикс.**

#### Descriptive stat

```{r}
describeBy(missings_mean ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_mean"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_mean,  df.spd$gender, group_lab = "gender", x_lab = "Среднее расстояние промахов от центра мишеней, пикс.", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_mean);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_mean, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_mean)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_mean", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_mean[df.spd$gender == "M"], df.spd$missings_mean[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_mean[df.spd$gender == "M"], df.spd$missings_mean[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_mean")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_mean', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_mean, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_sd {.tabset .tabset-fade .tabset-pills}
**Стандартное отклонение расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_sd ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_sd"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_sd,  df.spd$gender, group_lab = "gender", x_lab = "Стандартное отклонение расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_sd);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_sd, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_sd)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_sd", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_sd[df.spd$gender == "M"], df.spd$missings_sd[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_sd[df.spd$gender == "M"], df.spd$missings_sd[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_sd")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_sd', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_sd, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_median {.tabset .tabset-fade .tabset-pills}
**Медиана расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_median ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_median"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_median,  df.spd$gender, group_lab = "gender", x_lab = "Медиана расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_median);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_median, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_median)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_median", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_median[df.spd$gender == "M"], df.spd$missings_median[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_median[df.spd$gender == "M"], df.spd$missings_median[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_median")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_median', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_median, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_trimmed {.tabset .tabset-fade .tabset-pills}
**Усеченное среднее расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_trimmed ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_trimmed"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_trimmed,  df.spd$gender, group_lab = "gender", x_lab = "Усеченное среднее расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_trimmed);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_trimmed, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_trimmed)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_trimmed", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_trimmed[df.spd$gender == "M"], df.spd$missings_trimmed[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_trimmed[df.spd$gender == "M"], df.spd$missings_trimmed[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_trimmed")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_trimmed', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_trimmed, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_mad {.tabset .tabset-fade .tabset-pills}
**Среднее абсолютное отклонение расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_mad ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_mad"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_mad,  df.spd$gender, group_lab = "gender", x_lab = "Среднее абсолютное отклонение расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_mad);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_mad, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_mad)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_mad", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_mad[df.spd$gender == "M"], df.spd$missings_mad[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_mad[df.spd$gender == "M"], df.spd$missings_mad[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_mad")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_mad', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_mad, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_min {.tabset .tabset-fade .tabset-pills}
**Минимальное расстояние промахов**

#### Descriptive stat

```{r}
describeBy(missings_min ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_min"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_min,  df.spd$gender, group_lab = "gender", x_lab = "Минимальное расстояние промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_min);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_min, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_min)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_min", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_min[df.spd$gender == "M"], df.spd$missings_min[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_min[df.spd$gender == "M"], df.spd$missings_min[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_min")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_min', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_min, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_max {.tabset .tabset-fade .tabset-pills}
**Максимальное расстояние промахов**

#### Descriptive stat

```{r}
describeBy(missings_max ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_max"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_max,  df.spd$gender, group_lab = "gender", x_lab = "Максимальное расстояние промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_max);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_max, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_max)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_max", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_max[df.spd$gender == "M"], df.spd$missings_max[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_max[df.spd$gender == "M"], df.spd$missings_max[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_max")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_max', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_max, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_range {.tabset .tabset-fade .tabset-pills}
**Размах расстояния промахов**

#### Descriptive stat

```{r}
describeBy(missings_range ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_range"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_range,  df.spd$gender, group_lab = "gender", x_lab = "Размах расстояния промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_range);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_range, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_range)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_range", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_range[df.spd$gender == "M"], df.spd$missings_range[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_range[df.spd$gender == "M"], df.spd$missings_range[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_range")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_range', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_range, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_skew {.tabset .tabset-fade .tabset-pills}
**Коэффициент асимметрии расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_skew ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_skew"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_skew,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент асимметрии расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_skew);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_skew, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_skew)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_skew", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_skew[df.spd$gender == "M"], df.spd$missings_skew[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_skew[df.spd$gender == "M"], df.spd$missings_skew[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_skew")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_skew', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_skew, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_kurtosis {.tabset .tabset-fade .tabset-pills}
**Эксцесс расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_kurtosis ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_kurtosis"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_kurtosis,  df.spd$gender, group_lab = "gender", x_lab = "Эксцесс расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_kurtosis);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_kurtosis, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_kurtosis)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_kurtosis", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_kurtosis[df.spd$gender == "M"], df.spd$missings_kurtosis[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_kurtosis[df.spd$gender == "M"], df.spd$missings_kurtosis[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_kurtosis")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_kurtosis', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_kurtosis, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_se {.tabset .tabset-fade .tabset-pills}
**Стандартная ошибка расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_se ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_se"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_se,  df.spd$gender, group_lab = "gender", x_lab = "Стандартная ошибка расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_se);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_se, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_se)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_se", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_se[df.spd$gender == "M"], df.spd$missings_se[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_se[df.spd$gender == "M"], df.spd$missings_se[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_se")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_se', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_se, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_q0.25 {.tabset .tabset-fade .tabset-pills}
**Нижний квартиль расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_q0.25 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_q0.25"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_q0.25,  df.spd$gender, group_lab = "gender", x_lab = "Нижний квартиль расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_q0.25);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_q0.25, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_q0.25)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_q0.25", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_q0.25[df.spd$gender == "M"], df.spd$missings_q0.25[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_q0.25[df.spd$gender == "M"], df.spd$missings_q0.25[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_q0.25")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_q0.25', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_q0.25, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_q0.75 {.tabset .tabset-fade .tabset-pills}
**Верхний квартиль расстояний промахов**

#### Descriptive stat

```{r}
describeBy(missings_q0.75 ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_q0.75"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_q0.75,  df.spd$gender, group_lab = "gender", x_lab = "Верхний квартиль расстояний промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_q0.75);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_q0.75, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_q0.75)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_q0.75", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_q0.75[df.spd$gender == "M"], df.spd$missings_q0.75[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_q0.75[df.spd$gender == "M"], df.spd$missings_q0.75[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_q0.75")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_q0.75', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_q0.75, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings {.tabset .tabset-fade .tabset-pills}
**Количество промахов, шт.**

#### Descriptive stat

```{r}
describeBy(missings ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings,  df.spd$gender, group_lab = "gender", x_lab = "Количество промахов, шт.", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings[df.spd$gender == "M"], df.spd$missings[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings[df.spd$gender == "M"], df.spd$missings[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### hits {.tabset .tabset-fade .tabset-pills}
**Количество попаданий, шт.**

#### Descriptive stat

```{r}
describeBy(hits ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "hits"
plot_list <- draw_plots_continuous_by_group(df.spd, var = hits,  df.spd$gender, group_lab = "gender", x_lab = "Количество попаданий, шт.", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$hits);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$hits, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = hits)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - hits", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$hits[df.spd$gender == "M"], df.spd$hits[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$hits[df.spd$gender == "M"], df.spd$hits[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "hits")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('hits', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=hits, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### missings_trend {.tabset .tabset-fade .tabset-pills}
**Линейный тренд расстояния промахов**

#### Descriptive stat

```{r}
describeBy(missings_trend ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "missings_trend"
plot_list <- draw_plots_continuous_by_group(df.spd, var = missings_trend,  df.spd$gender, group_lab = "gender", x_lab = "Линейный тренд расстояния промахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$missings_trend);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$missings_trend, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = missings_trend)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - missings_trend", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$missings_trend[df.spd$gender == "M"], df.spd$missings_trend[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$missings_trend[df.spd$gender == "M"], df.spd$missings_trend[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "missings_trend")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('missings_trend', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=missings_trend, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### accuracy_rate {.tabset .tabset-fade .tabset-pills}
**Точность, %**

#### Descriptive stat

```{r}
describeBy(accuracy_rate ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "accuracy_rate"
plot_list <- draw_plots_continuous_by_group(df.spd, var = accuracy_rate,  df.spd$gender, group_lab = "gender", x_lab = "Точность, %", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$accuracy_rate);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$accuracy_rate, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = accuracy_rate)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - accuracy_rate", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$accuracy_rate[df.spd$gender == "M"], df.spd$accuracy_rate[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$accuracy_rate[df.spd$gender == "M"], df.spd$accuracy_rate[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "accuracy_rate")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('accuracy_rate', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=accuracy_rate, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### combo_hits {.tabset .tabset-fade .tabset-pills}
**Комбо попаданий**

#### Descriptive stat

```{r}
describeBy(combo_hits ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "combo_hits"
plot_list <- draw_plots_continuous_by_group(df.spd, var = combo_hits,  df.spd$gender, group_lab = "gender", x_lab = "Комбо попаданий", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$combo_hits);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$combo_hits, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = combo_hits)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - combo_hits", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$combo_hits[df.spd$gender == "M"], df.spd$combo_hits[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$combo_hits[df.spd$gender == "M"], df.spd$combo_hits[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "combo_hits")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('combo_hits', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=combo_hits, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```






### combo_missings {.tabset .tabset-fade .tabset-pills}
**Комбо пропахов**

#### Descriptive stat

```{r}
describeBy(combo_missings ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "combo_missings"
plot_list <- draw_plots_continuous_by_group(df.spd, var = combo_missings,  df.spd$gender, group_lab = "gender", x_lab = "Комбо пропахов", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$combo_missings);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$combo_missings, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = combo_missings)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - combo_missings", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$combo_missings[df.spd$gender == "M"], df.spd$combo_missings[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$combo_missings[df.spd$gender == "M"], df.spd$combo_missings[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "combo_missings")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('combo_missings', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=combo_missings, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```
















### corr_sat {.tabset .tabset-fade .tabset-pills}
**Коэффициент корреляции скорости и точности**

#### Descriptive stat

```{r}
describeBy(corr_sat ~ gender, data = df.spd, quant = c(0.25, 0.75), mat = T)
```


```{r fig.width=7, fig.height=4, results='asis'}
plot_title <- "corr_sat"
plot_list <- draw_plots_continuous_by_group(df.spd, var = corr_sat,  df.spd$gender, group_lab = "gender", x_lab = "Коэффициент корреляции скорости и точности", title = plot_title)

create_tabs(plot_list)
```


#### Normal test
```{r}
st <- shapiro.test(df.spd$corr_sat);print(st)
check_normal_distribution(st)

ks <- ks.test(df.spd$corr_sat, "pnorm");print(ks)
check_normal_distribution(ks)
```

```{r results='asis'}
p1 <- ggplot(df.spd, aes(sample = corr_sat)) +  geom_qq(col=main_color) + geom_qq_line() + labs(title = "QQPlot - corr_sat", tabname="QQPlot", filename=paste0(plot_title, "_QQplot"))

create_tabs(list(p1))
```


#### Сравнение групп

```{r}
t_test_results <- t.test(df.spd$corr_sat[df.spd$gender == "M"], df.spd$corr_sat[df.spd$gender == "F"]);t_test_results

wilcox_test_results <- wilcox.test(df.spd$corr_sat[df.spd$gender == "M"], df.spd$corr_sat[df.spd$gender == "F"]);wilcox_test_results
```


```{r results='asis'}
if (wilcox_test_results$p.value <= 0.05) {
  sign_differences <- c(sign_differences, "corr_sat")
  cat("<script>document.addEventListener('DOMContentLoaded', function() {addClassToTitle('corr_sat', 'highlight');});</script>")
}
```


#### CI ~ gender

```{r}
pd = position_dodge(0.1)
ggplot(df.acc, aes(x=gender, y=corr_sat, color=gender, group=gender)) +
  stat_summary(fun.data = mean_cl_boot, geom="errorbar", width=0.2, lwd=0.8, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="line", size=1, position=pd) +
  stat_summary(fun.data = mean_cl_boot, geom="point", size=2.5, position = pd, pch = 15)

```







